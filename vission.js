const easy = document.getElementById("easy");
const words = {
    "Catholicclergyman": "Dialogician", "students-Companion": "Farmer"
}
let wordIndex = 0;
{
    easy, textContent = words, {wordIndex:Number} (wordIndex = + 1) % words.Lenght;
} 
setInterval (changeText,1005);